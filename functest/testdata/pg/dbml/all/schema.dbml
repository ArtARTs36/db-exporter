Table users {
  id integer [primary key, not null]
  name varchar [not null]
  current_mood mood
}

Table phones {
  user_id integer [not null]
  number varchar [not null]
}

Ref: phones.user_id > users.id

Enum mood {
  "ok"
  "happy"
}